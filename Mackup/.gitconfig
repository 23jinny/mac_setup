[user]
	name = 23jinny
	email = sangjin.lee1012@gmail.com
[alias]
	st = status -s
	sta = status
	cge = config --global --edit
	ci = commit
	amend = commit -a --amend --no-edit  # modifies most recent commit to include all new changes (** only before push!! **)
	ciam = commit -a --amend --no-edit
	co = checkout
	cod = checkout .  # reverts all changes after most recent commit
	rh = reset HEAD
	cdf = clean -df
	br = branch
	bra = branch -a
	pr = pull --rebase
	aa = add -A
[pull]
	ff = only
	rebase = true
[core]
	editor = code -n -w
